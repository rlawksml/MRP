const { Component } = require('react');
const React = require('react');
const ReactDOM = require('react-dom');
const Home = require('./src/Home.jsx');

ReactDOM.render(<Home />, document.querySelector('#root'));
